import React from 'react';

const Player = () => {
    // const {name,position,club,country,transferredvalue,imageUrl} =props.Player
    return (
        <div>
         
        </div>
    );
};

export default Player;